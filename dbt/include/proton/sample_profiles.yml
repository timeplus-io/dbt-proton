default:
  outputs:

    dev:
      type: proton
      schema: [database name]
      host: [host]

      port: [port]
      user: [user]
      password: [password]

    prod:
      type: proton
      schema: [database name]
      host: [host]

      port: [port]
      user: [user]

  target: dev